/target
.DS_Store

# Built binaries — artifacts, not source. Kept on disk (build.sh writes
# them) but out of git; they bloated history at ~7MB per commit.

# Local Liv reference material — never part of the liv repo
/Programming the productivity app/

# editor droppings
\#*\#
.#*

# The iOS shell build artifact — same rule as the macOS one.
/shell/ios/build/
